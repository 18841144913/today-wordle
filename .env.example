# Public site URL (no trailing slash). Used for canonical URLs, OG tags, sitemap.
NEXT_PUBLIC_SITE_URL=https://today-wordle.com

# Secret protecting /api/revalidate. Set the same value in Vercel project env;
# Vercel Cron will send it automatically as a Bearer token.
CRON_SECRET=
